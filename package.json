{
  "private": true,
  "name": "viboplr-google-tests",
  "description": "Test harness for the viboplr-google plugin (not published; the plugin itself is just index.js + manifest.json).",
  "scripts": {
    "test": "node --test"
  }
}
